#include "global.h"
#include "parser/parser.h"
int main() {

	//vector<int> v = {/* 0, 1,2,3,1, 2, 3 };
	//vector<int>::iterator it =  remove(v.begin(), v.end(), 2);
	//v.erase(it, v.end());
	//for (int i = 0; i < */v.size(); ++i) cout << v[i] << ' ';
	RBparse();
}